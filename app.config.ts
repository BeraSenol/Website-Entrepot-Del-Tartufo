export default defineAppConfig({
        ui: {
                primary: 'white',
                gray: 'neutral',
                container: {
                        constrained: "max-w-full"
                }
        }
})