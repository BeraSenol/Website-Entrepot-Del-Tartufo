export default defineAppConfig({
        ui: {
                primary: 'blue',
                gray: 'slate',
                container: {
                        constrained: "max-w-full"
                }
        }
})